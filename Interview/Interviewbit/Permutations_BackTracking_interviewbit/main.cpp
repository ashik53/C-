#include <bits/stdc++.h>

using namespace std;

/*void backTrack(vector<intA){

} */

int main()
{
    vector<vector<int> > vec;
    vector<int> h;
    h.push_back(1);
    vec.push_back(h);

    h[0] = 10;
    cout<< h[0] << endl;
    /*vector<int> A;

    int sz;

    while(cin >> sz){
        A.clear();

        for(int i = 0; i < sz; i++){
            int ch;
            scanf("%d",&ch);
            A.push_back(ch);
        }

        backTrack(A);
    }
    */

}



